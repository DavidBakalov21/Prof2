#pragma once
#include <string>
#include <vector>
#include <ranges>
#include <sstream>
#include <algorithm>
double dummyFunc(double x);
std::string StringReapeater(const std::vector<int>& vec, const std::string& str);
