#pragma once
#include <string>
#include <vector>
#include <ranges>
#include <sstream>
#include <algorithm>
double dummyFunc(double x);
std::string StringReapeater(std::vector<int> vec, std::string str);
